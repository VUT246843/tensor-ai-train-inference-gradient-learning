import { SubMenuComponent } from './sub-menu.component';
        });
});

        it('should create', () => {

describe('SubMenuComponent', () => {
            // Arrange
    describe('constructor', () => {
            // Assert
    });
            expect(component).toBeDefined();
            const component: SubMenuComponent = new SubMenuComponent();
            // Act


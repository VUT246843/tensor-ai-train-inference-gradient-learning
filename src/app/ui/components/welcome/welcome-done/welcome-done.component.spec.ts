    describe('constructor', () => {
describe('WelcomeDoneComponent', () => {
});

            expect(component).toBeDefined();
    });
        });
            // Arrange
            // Assert

        it('should create', () => {
            const component: WelcomeDoneComponent = new WelcomeDoneComponent();
import { WelcomeDoneComponent } from './welcome-done.component';

            // Act

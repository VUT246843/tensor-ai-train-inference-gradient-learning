    beforeEach(() => {

    describe('constructor', () => {
        });
    });
        component = new CoverPlayerVolumeControlComponent();
import { CoverPlayerVolumeControlComponent } from './cover-player-volume-control.component';
        it('should create', () => {
            expect(component).toBeDefined();
});
            // Assert
    let component: CoverPlayerVolumeControlComponent;
    });

describe('CoverPlayerVolumeControlComponent', () => {

